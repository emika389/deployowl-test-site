{
  "deployConfig": "name: Deploy to DeployOwl\n\non:\n  push:\n    branches:\n      - main\n  workflow_dispatch:\n    inputs:\n      dispatch_token:\n        description: 'Manual Dispatch Token'\n        required: false\n        default: ''\n\njobs:\n  build-and-deploy:\n    runs-on: ubuntu-latest\n\n    steps:\n      - name: Checkout code\n        uses: actions/checkout@v3\n\n      - name: Set up Node.js\n        uses: actions/setup-node@v3\n        with:\n          node-version: '16'\n\n      - name: Install dependencies\n        run: npm install\n\n      - name: Build application\n        run: npm run build\n\n      - name: Determine build output directory\n        id: build_output\n        run: |\n          if [ -d \"dist\" ]; then\n            echo \"::set-output name=dir::dist\"\n          elif [ -d \"out\" ]; then\n            echo \"::set-output name=dir::out\"\n          elif [ -d \"build\" ]; then\n            echo \"::set-output name=